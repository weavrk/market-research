# GoDaddy Python App - Minimal .htaccess
# GoDaddy's Python App setup handles most configuration automatically
# This file is kept minimal to avoid conflicts

# Prevent access to sensitive files
<FilesMatch "^\.">
    Order allow,deny
    Deny from all
</FilesMatch>

# Prevent access to Python cache
<FilesMatch "\.pyc$">
    Order allow,deny
    Deny from all
</FilesMatch>
